# gstock-api

REST API for **gstock** inventory management. Built with NestJS 11 + Prisma 6 + PostgreSQL (Supabase). Deployed to Render.

The full README is generated in Task 17 once the project is feature-complete.
